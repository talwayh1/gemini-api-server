services:
  gemini-api:
    image: ghcr.io/talwayh1/gemini-api-server:latest
    container_name: gemini-api
    restart: unless-stopped
    ports:
      - "8787:8000"
    volumes:
      - ./config:/app/config:ro
      - ./cache:/app/cache
    environment:
      - GEMINI_COOKIE_PATH=/app/cache
